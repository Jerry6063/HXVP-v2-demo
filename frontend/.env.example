# 复制为 .env.local 并按需填写。
# 留空时前端会请求 /api（vite dev 代理到 localhost:8000，但本项目 v2 暂无后端，
# 因此 API 调用会失败 —— 这是预期的，等迁移到 mock/新后端再填）。
VITE_API_BASE_URL=

# 可选：hCaptcha site key（v2 还未接入，留空即可）
VITE_HCAPTCHA_SITE_KEY=
